<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Records</title>
  <link href="/public/styles/records.css" rel="stylesheet" />
</head>

<body>
  <main>
    <h1 id="page_title">
      Records
    </h1>


    <div class="card">
      <font><b>#</b></font>
      <font><b>Name</b></font>
      <font><b>ID</b></font>
    </div>

    <% for(let i=0; i < records.length; i++) { %>
      <div class="card">
        <font>
          <%=records[i].id %>
        </font>
        <font>
          <%=records[i].stu_id %>
        </font>
        <font>
          <%=records[i].name %>
        </font>
      </div>
      <% } %>
  </main>
</body>

</html>